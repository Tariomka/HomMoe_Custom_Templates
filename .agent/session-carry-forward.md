# Session Carry-Forward

## Session goal

Complete Phase 5 of `plans/clean-architecture-refactoring.md`: consolidate neutral and hub zone creation into shared injected factories without changing generated output, editor behavior, or coverage.

## Fixes applied

- Extracted shared zone, castle, and road construction into `internal/services/zones`.
- Removed the manual editor's dependency on `template_generator/providers/topology/base`.
- Passed explicit `Hub` and `Hub-<label>` names into zone creation and removed tournament post-build renaming.
- Propagated one `CreationServices` graph through generation, topology, mandatory-content, and editor composition.
- Restored spawn-zone prefix ownership to `constants.PlayerZonePrefix`.
- Added public-path coverage for the live zone-size NaN, infinity, clamp, and rounding behavior.

## Features added / changed

- Added passive `NeutralZoneCreation`, `HubZoneCreation`, `NeutralLikeZoneCreation`, `ZoneCastleStrategy`, and `ZoneBiomeMatchPolicy` models.
- Added `ZoneFactory`, `CastleFactory`, `RoadFactory`, and `CreationServices`.
- Neutral and hub wrappers share one private profile-based zone construction path while retaining distinct castle, mandatory-content, outpost, foothold, randomization, biome, and road policies.
- Existing constructors and `TopologyBase` forwarding methods remain compatibility shims for Phase 8.
- No product behavior or serialized schema changed.

## File modifications

Created production files:

- `internal/models/hubZoneCreation.go`
- `internal/models/neutralLikeZoneCreation.go`
- `internal/models/neutralZoneCreation.go`
- `internal/models/zoneBiomeMatchPolicy.go`
- `internal/models/zoneCastleStrategy.go`
- `internal/services/zones/castleFactory.go`
- `internal/services/zones/creationServices.go`
- `internal/services/zones/roadFactory.go`
- `internal/services/zones/zoneFactory.go`
- `internal/services/zones/zoneFactoryNeutralLike.go`

Edited production files:

- `internal/handlers/guiHandler.go`
- `internal/services/connection_editor/zoneEditorService.go`
- `internal/services/template_generator/templateGenerator.go`
- `internal/services/template_generator/providers/mandatoryContentProvider.go`
- `internal/services/template_generator/providers/topologyProvider.go`
- `internal/services/template_generator/providers/topology/base/topologyBase.go`
- All topology constructor files under `internal/services/template_generator/providers/topology/` that receive shared creation services, including tournament cluster services.

Plan and handoff:

- `plans/clean-architecture-refactoring.md` - Phase 5 marked Complete with final verification.
- `.agent/session-carry-forward.md` - replaced with this Phase 5 handoff.

Test changes:

- Added mirrored constructor-injection tests for `TemplateGenerator`, `MandatoryContentProvider`, `ZoneEditorService`, `TopologyBase`, every topology provider/service, and tournament cluster service.
- Added `test/unit/internal/services/zones/{zoneFactory,castleFactory,roadFactory,creationServices}/`.
- Expanded neutral/hub `TopologyBase` characterization tests.
- Added live zone-size normalization tests through `ZoneFactory.CreateNeutralZone`.

## Tests added or updated

Latest verified state after final Opus review fixes:

- Unit coverage command: passed at 64.9%, above the 64.7% Phase 5 baseline.
- `go build ./...`: passed.
- `go test ./test/... -count=1`: passed.
- `go test -tags=integration_test ./test/integration/... -count=1`: passed.
- `go test -tags='integration_test,gui' ./test/integration/gui/... -count=1`: passed.
- `go test -tags=integration_test ./test/performance/... -count=1`: passed (`no tests to run`).
- `go test ./test/unit/internal/services/template_generator/... -count=20`: passed.
- Focused lint for new zone services and final `TemplateGenerator` composition: zero issues.
- Code diagnostics for changed code, architecture searches, protected-path check, and `git diff --check`: passed.
- Repository-wide diagnostics still report pre-existing Markdown lint in `AGENTS.md`; it was not modified.
- Claude Opus 5 final review found two hard issues; both were fixed and focused/full tests rerun successfully.

## Git status snapshot

- Branch: `AD/refactoring-07-21`.
- All Phase 5 changes are unstaged; new model/service/test files are untracked.
- No files were staged, committed, reverted, or stashed by the agent.
- `coverage.txt` was regenerated by required coverage checks.
- No change appears under `data/`, `internal/entities/template/`, or `internal/registry/`.
- Use `git status --short` for the exact inherited listing before Phase 6.

## Rejections / things the user declined

- No user proposal was declined in Phase 5.
- The review suggestion to hide hub guard randomization as a factory default was rejected because `HubZoneCreation` intentionally makes it explicit and every production caller supplies the preserved `0.05` value.
- Deleting compatibility `TopologyBase` methods and the old base size helper was deferred to the already-planned Phase 8 shim cleanup.
- The VS Code targeted test runner did not discover one mirrored external Go test file; the owning package was run directly and passed.

## Open questions

- Phase 5 has no blocker and is marked Complete.
- Phase 8 should remove compatibility `TopologyBase` forwarding functions, the dead duplicate base size helper/tests, and other migration shims.
- A zero-value manually constructed `CreationServices` can contain nil exported fields; production constructors do not do this. Consider validation or private fields during Phase 8 architecture locking.
- The hold-city castle invariant during manual reapply predates Phase 5 and was not changed; reassess only within a separately scoped behavior task.

## Next recommended actions

1. Read `AGENTS.md`, Phase 6 in the plan, and this handoff.
2. Recheck `git status --short` and preserve the unstaged Phase 5 worktree.
3. Commit Phase 5 if requested; otherwise record a fresh Phase 6 coverage baseline before editing.
4. Start Phase 6 with builder-owned connection literals and positioned topology composition characterization.
5. Keep Phase 8 compatibility cleanup deferred unless Phase 6 directly removes a caller.

## Carry-forward prompt

Read `AGENTS.md` first, then read `plans/clean-architecture-refactoring.md` and `.agent/session-carry-forward.md`. Never modify `data/`, `internal/entities/template/`, or `internal/registry/`; preserve Windows/Linux portability; add tests for non-trivial logic and do not reduce coverage. Phase 5 is Complete and Opus-reviewed at 64.9% coverage: neutral, hub, spawn, castle, and road creation now use shared injected services; manual editing no longer imports topology base; tournament hubs receive final names directly; compatibility shims remain for Phase 8. Resume from the current unstaged worktree at Phase 6 only after checking status and recording its baseline. Use `.agent/session-carry-forward.md` for the full handoff.
